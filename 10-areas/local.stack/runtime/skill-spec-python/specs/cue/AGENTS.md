# CUE Model

This directory is the normalized model layer.

## Rules

- prefer explicit constraints over prose notes
- keep definitions reusable and stable
- treat these files as the generation control surface
