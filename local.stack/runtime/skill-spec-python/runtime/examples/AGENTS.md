# Runtime Examples

This directory holds checked example configs for runtime ingress adapters.

## Rules

- keep examples small and deterministic
- prefer one valid example per artifact family
- use these examples in bounded runtime checks
